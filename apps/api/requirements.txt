# Backend setup (run once, from apps/api):
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements.txt
# Start the API (each time, leave it running; the web app's Add material needs it):
#   .venv/bin/uvicorn app.main:app --reload --port 8000
# Run the tests:
#   .venv/bin/python -m pytest -q
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.5,<3.0
jsonschema>=4.20,<5.0
pytest>=8.0,<9.0
httpx>=0.26,<1.0
python-multipart>=0.0.9,<1.0
pypdf>=4.0,<6.0
